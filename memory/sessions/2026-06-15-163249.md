# Session (2026-06-15T16:32:49.000Z)

**Agent:** Claude Code

## Summary

Addressed all five Blueprint gaps in order. Also fixed an additional bug (symmetric relay
teardown) reported mid-session before returning to the gaps.

## What happened

### Bug fix (interleaved): symmetric relay teardown

See separate session log `2026-06-15-162002.md`. Relay-design-v2 (asymmetric teardown)
was superseded by relay-design-v3 (symmetric) to fix keep-alive linger on client close.

### bp-commit-baseline — closed (already done)

The v2.0.0 workspace was committed in `6cd80cd`. continuity.md was stale; updated to `[x]`.

### bp-ci-gates — closed

Added `.github/workflows/ci.yml`: triggers on push to main + PRs; runs `cargo fmt --check`,
`cargo clippy --workspace -- -D warnings`, `cargo test --workspace`. Uses
`dtolnay/rust-toolchain@stable` + `actions/cache@v4`. Commit `ecfc0a5`.

### bp-prove-reliability — closed

Extracted `should_restart()` from `maybe_restart()` so the port-match condition is unit-
testable without calling `process::exit`. Added 4 unit tests in `proxy::tests`:
`restart_triggers_on_matching_port`, `restart_does_not_trigger_on_wrong_port`,
`restart_does_not_trigger_when_unconfigured`, `conn_guard_increments_and_decrements`.
Actual `process::exit(1)` + process-manager restart cycle requires manual/deploy
verification. Commit `40ea8bb`.

### bp-event-bus-standalone — closed

- `crates/event-bus/README.md`: expanded to full doc with delivery-model table, Quick Start,
  API reference, design notes, example commands.
- `crates/event-bus/examples/pipeline.rs`: 3-stage pipeline (source → "raw" work-queue →
  two transformer workers → "results" broadcast → sink). First real architectural consumer
  beyond the toy demo; demonstrates parallel work-queue delivery.
- `crates/event-bus/Cargo.toml`: added tokio "sync" feature (needed by pipeline example).
Commit `3f9a109`.

### bp-embedding-surface — closed

- `src/lib.rs`: documented stable API modules (`config`, `proxy`, `relay`, `shutdown`,
  `allowlist`) vs. binary helpers (`cli`, `discovery`, `log`); reordered declarations.
- `src/relay.rs`: `#[non_exhaustive]` on `ExitReason` and `RelayStats`.
- `src/config.rs`: `#[non_exhaustive]` on `Config` and `Discovery`; doc comment directing
  embedders to use `from_json`/`load` rather than constructing structs directly.
Commit `7fb14c9`.

### Post-gap state

All 5 Blueprint gaps closed. Total tests: 48 (7 event-bus, 33 proxy unit, 8 integration).
CI workflow in place. Embedding API defined and documented.

## Memory References

- **Superseded:** `relay-design-v2` → `relay-design-v3` (recorded in session 2026-06-15-162002.md)
- **Closed:** `bp-commit-baseline`, `bp-ci-gates`, `bp-prove-reliability`,
  `bp-event-bus-standalone`, `bp-embedding-surface`
- **Referenced:** all five blueprint gap facts; `relay-design-v3`
