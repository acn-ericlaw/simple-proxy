# Session (2026-06-15T18:17:56.000Z)

**Agent:** Claude Code

## Summary

Resolved the one outstanding item — the VBDI re-horizon **human gate**
`ot-vision-gap-closed-rehorizon`, raised by the 2026-06-15 review when the
`vision-simple-proxy` Vision↔Current-State gap closed. The maintainer confirmed the
simple-proxy Vision as **realized** and chose to **promote the in-process `event-bus` crate
to the new Vision**. Per `AGENTS.md` / `DECAY.md` §12 (altitude transitions are human-gated
and the Vision must never be fabricated), I asked the maintainer to set the two scope
decisions, then wrote the new Vision + Blueprint they approved. Only `memory/` files were
touched — no source, manifests, README, or any non-`memory/` file.

## Maintainer decisions (the human gate)

1. **Next horizon:** promote the larger event-bus project to the new Vision.
2. **Bus scope:** *mature the in-process library* — keep it minimal, embeddable, and
   in-process; harden it (bounded channels + backpressure, typed-optional payloads,
   observability, route lifecycle, semver-stable & published). Networking is explicitly OUT
   of scope (a future Vision, opened through a fresh gate — not this one).
3. **simple-proxy's role:** *stays independent* — v2.0.0 is realized and in maintenance for
   its core L4 forwarding. It is NOT the bus's real-world consumer, but it IS the planned
   **demonstration / experiment**: wire the event-bus into the proxy's *control plane*
   (connection open/close signaling), never the byte data path, as a **separate example /
   feature-gated artifact** so the shipping binary stays flume-free (`minimal-deps` core
   invariant). Learn from the experiment to refine the bus before adopting a real-world
   consumer (which must be something other than simple-proxy). *(Refined from the initial
   draft per maintainer feedback — the control-plane signaling demo + proving path were added
   after the first vision write.)*

## What happened

### 1. New Vision (`memory/vision.md`)
Rewrote `vision.md` as `vision-event-bus` (`tier: core`, `supersedes: vision-simple-proxy`):
elevator, current-state (grounded in the real v0.1.0 crate — flume, broadcast + work-queue,
`Vec<u8>`, sync non-blocking publish, **unbounded** channels, no typed layer, no metrics, no
route teardown, examples-only consumers), target, success criteria, non-goals (never
networked, never an app, never a heavy dep tree), a **Proving path** (experiment via a
simple-proxy control-plane signaling demo → learn/refine → real-world consumer), mental model,
and a Lineage note recording that `vision-simple-proxy` was realized (not wrong).

### 2. Archived the realized prior Vision
`vision-simple-proxy` moved to `memory/archive/2026-Q2.md` under a new "Vision re-horizon"
heading, flagged `tier: superseded | superseded-by: vision-event-bus`,
`reason: realized; superseded by vision-event-bus`. Added a row to `archive/INDEX.md`. This
is the one superseded fact that was *true-and-achieved* rather than invalidated; the link is
preserved, it is terminal (never reactivates) per `DECAY.md` §9.

### 3. continuity.md
- Project State `status:` rewritten — simple-proxy v2.0.0 realized & in maintenance; active
  horizon = event-bus maturation; `last_session` set to today + Claude Code.
- Marked `ot-vision-gap-closed-rehorizon` `[x]` with the resolution recorded in place (per
  protocol, left in place for the review to sweep).
- Relabelled the old Blueprint subsection "Blueprint (realized) — vision-simple-proxy" (all
  `[x]`, left in place until swept past `archive_window` 20).
- Opened a new **"Blueprint — Phase 1: mature the in-process event-bus"** subsection with
  seven `(blueprint)` gaps, each `serves: vision-event-bus`, each grounded in a real gap I
  verified by reading `crates/event-bus/src/lib.rs` + README + examples.

### 4. New Blueprint gaps (all born `tier: working`)
- `bp-eb-bounded-backpressure` — bounded channels + overflow policy (block/drop-oldest/reject).
- `bp-eb-typed-payloads` — feature-gated typed/serde layer over the `Vec<u8>` core.
- `bp-eb-observability` — per-route metrics (queue depth, delivered/dropped, subscriber/worker).
- `bp-eb-route-lifecycle` — route/queue teardown so the maps don't grow unbounded.
- `bp-eb-proxy-signaling-demo` — wire event-bus control-plane signaling (connection
  open/close lifecycle) into a simple-proxy *demo* (separate example / feature-gated; shipping
  binary stays flume-free; data path direct) — the learning experiment.
- `bp-eb-real-consumer` — ≥1 real-world integration (not simple-proxy, not a demo), sequenced
  after and informed by the experiment.
- `bp-eb-publish-stable` — settle the 1.0 surface, docs/CHANGELOG, publish.

### 5. Cadence checks
- **Review:** not due — this is session 1 since `last_review` (through 2026-06-15-172641.md),
  `review_every` 10; `continuity.md` is 208 lines (< `continuity_max_lines` 300).
- **Invariant verification:** not due (now 11 session files < `verify_invariants_every` 20).
- Invariants unchanged (the three `core` proxy invariants still hold; the minimal-deps ethos
  carries into the new Vision's non-goals).

## Notes / open items

- The Phase-1 Blueprint is now the active VBDI gap. Next design/implementation work should
  tie to one of the `bp-eb-*` ids and trace to `vision-event-bus`.
- **Maintainer confirmed** the signaling-demo constraint: a separate example / feature-gated
  artifact, no flume in the shipping `simple-proxy` binary (pinned on `bp-eb-proxy-signaling-demo`).
  Re-opening it requires an explicit `minimal-deps` core-invariant change.
- `archive-candidate` JS-era backlog threads + the realized `bp-*` gaps remain in
  `continuity.md`; they sweep automatically at a future review once past the 20-session window.
- Reminder: commit `memory/` — `git add memory/ && git commit -m "session 2026-06-15 [Claude Code]"`.

## Memory References
- Referenced: vision-event-bus, ot-vision-gap-closed-rehorizon, event-bus-module,
  bp-event-bus-standalone, bp-embedding-surface, bp-commit-baseline, bp-ci-gates,
  bp-prove-reliability, minimal-deps, layer-4-only
- Created: vision-event-bus (tier: core), bp-eb-bounded-backpressure, bp-eb-typed-payloads,
  bp-eb-observability, bp-eb-route-lifecycle, bp-eb-proxy-signaling-demo, bp-eb-real-consumer,
  bp-eb-publish-stable (all tier: working except the Vision)
- Superseded: vision-simple-proxy → vision-event-bus (archived flagged superseded — realized, not invalidated)
