# Session (2026-06-13T17:51:00.000Z - 2026-06-13T18:15:02.000Z)

**Agent:** Claude Code
**User:** Remove the Node-era PM2 references; then investigate adopting a "named route" Tokio event-bus pattern (ref `~/sandbox/rust/rust_event_bus_example`) and build a general-purpose event bus with byte-array payloads.

## What We Did

Removed all PM2 references across the repo (README service section now uses a systemd
unit; CHANGELOG/proxy.rs comment/memory wording made manager-agnostic). Investigated the
reference event-bus pattern: a named-route flume dispatcher. Concluded the proxy's hot
byte-relay should stay direct (a bus there is pure overhead), so the right move is a
standalone reusable module. Built `src/event_bus.rs`: a general-purpose named-route event
bus on flume with `Vec<u8>` payloads, supporting BOTH broadcast (pub/sub via `subscribe`)
and work-queue (`worker`) delivery, which can coexist per route; `publish` is
sync/non-blocking and returns a `Delivered { subscribers, queued }`. Added 7 unit tests +
`examples/event_bus_demo.rs`. Full suite (36 unit + 5 integration), fmt, clippy all green;
demo runs and shows fan-out + work-sharing + binary payloads.

## Decisions Made

- Standalone module only — do NOT route the proxy's byte stream through the bus.
- Support both delivery modes (broadcast + queue), coexisting per route (NATS-like).
- Backend: `flume` (matches the reference), accepted as an addition to the minimal-deps
  invariant; it's used by the event-bus module only, not the proxy.
- Payload is `Vec<u8>` (binary-safe), per the user's requirement.

## Context for Next Session

`src/event_bus.rs` is self-contained and liftable into the bigger event-bus project.
Possible next steps if asked: bounded channels / backpressure, `Arc<[u8]>` payloads to
avoid per-subscriber clone on fan-out, dynamic unsubscribe, or extracting it into its own
workspace crate (so the proxy binary doesn't link flume). Everything builds and tests
green; all changes (Rust rewrite + pm2 removal + event bus) remain **uncommitted**.

## Memory References
- Referenced:  minimal-deps, relay-design, graceful-shutdown, logging-convention
- Created:     event-bus-module
