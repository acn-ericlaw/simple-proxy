# Session (2026-06-15T19:04:08.000Z)

**Agent:** Claude Code

## Summary

Implemented the first Phase-1 Blueprint gap, `bp-eb-proxy-signaling-demo`: enhanced
simple-proxy to emit **control-plane** connection-lifecycle signals through a hook, and
added a worked `event_bus_signaling` example that bridges those signals onto the
`event-bus` crate. simple-proxy keeps its exact functionality; the byte relay is untouched;
the shipping binary links no flume. Realizes `conn-observer-hook`; traces to
`vision-event-bus`.

## Design (tied to the locked constraints)

- A **zero-dependency observer hook** in the proxy lib + an **example** that bridges it to
  the bus. This satisfies every confirmed constraint: shipping binary stays flume-free
  (event-bus is a **dev-dependency**, only in the example), `relay()` byte path untouched
  (events fire *around* it — control plane only; `layer-4-only` preserved), and the existing
  public API stays intact (additive only).

## What was built

- **`src/observer.rs`** (new, pure std): `ConnEvent` enum (`Rejected`, `UpstreamUnavailable`,
  `Opened`, `Closed { rx, tx, reason }` — `#[non_exhaustive]`, `Debug + Clone`), the
  `ConnObserver` trait, and `NoopObserver`.
- **`src/proxy.rs`**: `handle_connection` now emits a `ConnEvent` at each lifecycle point
  (rejected / upstream-unavailable / opened / closed). `serve_listener` keeps its 7-arg
  signature and delegates to a new additive `serve_listener_observed(... observer ...)` with a
  `NoopObserver` — so `main.rs` and existing integration tests are unchanged and the CLI links
  nothing extra.
- **`src/relay.rs`**: added `ExitReason::label()` → stable `"closed"/"idle"/"shutdown"/"error"`.
- **`src/lib.rs`**: exported `pub mod observer;` and documented the hook +
  `serve_listener_observed` as part of the stable embedding surface.
- **`Cargo.toml`**: `event-bus` added under `[dev-dependencies]` (NOT a normal dep).
- **`examples/event_bus_signaling.rs`**: `BusObserver` publishes each event to the bus —
  broadcast route `conn.events` (a live monitor prints them) and work-queue route
  `conn.closed` (a metrics worker tallies connections + bytes). Drives 3 real echo round-trips
  through the actual proxy + 1 rejected connection through a locked-down second proxy.
- **`tests/integration.rs`**: two new tests via a `CollectingObserver` —
  `observer_receives_open_and_close_events`, `observer_receives_rejected_event`.
- **`README.md`**: new "Embedding: control-plane events (event-bus example)" section.

## Verification

- `cargo fmt --all --check` clean; `RUSTFLAGS="-D warnings" cargo clippy --workspace
  --all-targets` clean (covers the example + tests).
- `cargo test --workspace` → **50 pass** (7 event-bus + 33 proxy unit + 10 integration; was 48).
- `cargo run --example event_bus_signaling` runs: proxy logs show real forwarding (`rx 6 tx 6`
  per round-trip); `[monitor]` shows OPENED/CLOSED/REJECTED off the bus; the work-queue worker
  aggregates 3 connections / 18 bytes each way (3 × "ping-N" = 3 × 6). Correct.

## Notes / open items

- Gap `bp-eb-proxy-signaling-demo` marked `[x]` (done & verified). Per `DECAY.md` §12 closing a
  gap is a human-gated transition — surfaced for maintainer review; the commit is **not pushed**
  yet, so review happens before it lands.
- Not committed/pushed — awaiting the maintainer's go-ahead. Suggested commit: feature
  (`src/` + `Cargo.toml` + `examples/` + `tests/` + `README.md`), then memory separately.
- Next Phase-1 gaps (bus internals): `bp-eb-bounded-backpressure`, `bp-eb-typed-payloads`,
  `bp-eb-observability`, `bp-eb-route-lifecycle`; then `bp-eb-real-consumer` (real-world),
  `bp-eb-publish-stable`.

## Memory References
- Referenced: vision-event-bus, bp-eb-proxy-signaling-demo, event-bus-module, minimal-deps,
  layer-4-only, bp-embedding-surface, relay-design-v3
- Created: conn-observer-hook (tier: working)
- Superseded: (none)
