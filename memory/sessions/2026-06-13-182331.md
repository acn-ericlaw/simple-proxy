# Session (2026-06-13T18:16:00.000Z - 2026-06-13T18:23:31.000Z)

**Agent:** Claude Code
**User:** Do the clean move — extract the event_bus module into its own workspace crate so the proxy binary doesn't link flume.

## What We Did

Converted the repo into a Cargo workspace (root `[workspace]`, resolver 2) and extracted
the event bus from `src/event_bus.rs` into a standalone member crate `crates/event-bus/`
(`event-bus` v0.1.0, lib at `src/lib.rs`, demo under `examples/`, own README). Removed
`flume` from the proxy's `Cargo.toml` and dropped `pub mod event_bus` from the proxy lib.
`flume` is now a dependency of the `event-bus` crate only (tokio is dev-only there, for the
example + async test). Verified isolation with `cargo tree`: `simple-proxy` links no flume;
`event-bus` → flume 0.11. Workspace build/fmt/clippy clean; all 41 tests pass (7 event-bus
+ 29 proxy unit + 5 integration); demo runs via `cargo run -p event-bus --example event_bus_demo`.

## Decisions Made

- Layout: root package + workspace; the new crate lives under `crates/event-bus/` (the
  proxy stays at the repo root). Simple-proxy is NOT a dependency of event-bus and vice versa.
- `tokio` is a dev-dependency of `event-bus` (the library itself needs only flume).

## Context for Next Session

Clean liftable crate for the bigger project. The proxy binary is flume-free (confirmed via
`cargo tree -p simple-proxy`). If the bigger project grows, simple-proxy itself could later
move under `crates/` for a fully symmetric virtual workspace. Everything (Rust rewrite +
pm2 removal + event bus + workspace extraction) is still **uncommitted**.

## Memory References
- Referenced:  event-bus-module, minimal-deps
- Created:     (none — updated the event-bus-module fact in place to reflect the crate move)
